import styles from "./hero.module.scss";
const Hero = () => {
    return (
        <main className={styles.hero}>
            <h1 className="bold-text">Hero Section</h1>
        </main>
    );
};

export default Hero;
